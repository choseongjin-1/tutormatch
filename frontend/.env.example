## Optional override. In dev, requests to /api are proxied to http://localhost:8080 by vite.config.ts,
## so this is normally only needed for production builds served from a different origin than the API.
# VITE_API_BASE_URL=https://api.example.com/api
